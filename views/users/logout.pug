extends ../layouts/main

block content
  .mx-auto.max-w-md.mt-10.text-center
    h1.text-2xl.font-bold.mb-4 Logged Out
    p.mb-4 You have been logged out.
    a.text-blue-600(href="/users/login") Login again
